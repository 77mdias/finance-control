# These will be automatically created by Neon Launchpad (https://neon.new).
# Para Docker local (docker-compose.dev.yml), use:
# VITE_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/finance_control?schema=public
VITE_DATABASE_URL=
VITE_DATABASE_URL_POOLER=

# Better Auth
# Generate a random secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# DATABASE_URL é necessário para algumas ferramentas Prisma mesmo que o schema use VITE_DATABASE_URL
DATABASE_URL=

# Cartões (criptografia AES-256-GCM). Use 32 bytes. Suporte: plain, hex:, base64:
# Exemplo: base64:$(openssl rand -base64 32)
CARD_ENCRYPTION_KEY=

# Docs de API (dev)
# Valores: "" (desabilitado), "true" (habilita), "auth" (exige sessão Better Auth)
ENABLE_API_DOCS=
