# BKD-003.b — Implementar GET /transactions (loader/serverFn)

- **ID:** BKD-003.b
- **Título:** Implementar `GET /transactions` loader/serverFn com filtros por mês/ano

## Checklist

- [x] Implementar Server Function que aceita query params (month, year, page, perPage)
- [x] Retornar lista ordenada por date desc e total count
- [x] Escrever testes unitários para filtros

## Detalhes

- **Prioridade:** 🔴 Crítica
- **Estimativa:** 2h
- **Dependências:** BKD-003.a, BKD-002
- **Arquivos alvo:** `src/server/transactions.server.ts`

### Notas de conclusão

- `listTransactions` implementada em `src/routes/transactions.server.ts` com filtros (`month/year/type/category/cardId/subscriptionId`) e paginação (`page/perPage`, máx. 50), ordenação `date desc, createdAt desc`.
- DTO inclui `items`, `total`, `hasNextPage`, `appliedFilters`.
- Testes unitários de filtros/ordenacão em `tests/unit/transactions.server.test.ts`.
