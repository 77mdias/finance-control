# BKD-003.c — Implementar POST /transactions

- **ID:** BKD-003.c
- **Título:** Implementar `POST /transactions` (validações + criação)

## Checklist

- [x] Validar campos obrigatórios (value, type, date)
- [x] Criar transação no Prisma vinculada ao userId da sessão
- [x] Atualizar saldo se aplicável (pré-cálculo no servidor)
- [x] Testes unitários para criação e validação

## Detalhes

- **Prioridade:** 🔴 Crítica
- **Estimativa:** 1.5h
- **Dependências:** BKD-003.a, BKD-001 (auth)
- **Arquivos alvo:** `src/routes/transactions.server.ts`

### Notas de conclusão

- `createTransaction` em `src/routes/transactions.server.ts` usa Zod (`value > 0`, `type`, `date`, `description`, `category`) e valida `cardId/subscriptionId` pertencentes ao usuário.
- Retorno inclui `transaction` DTO e `balanceDelta` (CREDIT = +valor; DEBIT = -valor).
- Testes unitários cobrindo validação/relacionamentos em `tests/unit/transactions.server.test.ts`.
