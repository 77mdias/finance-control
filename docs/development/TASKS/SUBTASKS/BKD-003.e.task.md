# BKD-003.e — Integrar loaders e cache

- **ID:** BKD-003.e
- **Título:** Integrar loaders ao TanStack Router e cache com QueryClient

## Checklist

- [x] Implementar loader que consome `GET /transactions` e fornece dados para rota
- [x] Configurar `QueryClient` e usar `useQuery`/`useLoaderData` conforme padrão do projeto
- [x] Testar comportamento de cache e refetch nas operações de mutação

## Detalhes

- **Prioridade:** 🔴 Crítica
- **Estimativa:** 1h
- **Dependências:** FE-007, BKD-003.b
- **Arquivos alvo:** `src/routes/transactions.*`, `src/lib/queryClient.ts`

### Notas de conclusão

- Rota `/transactions` criada em `src/routes/transactions.tsx` com `validateSearch` reutilizando `normalizeTransactionFilters` e loader que faz `queryClient.ensureQueryData(transactionsQueryOptions)`.
- Componente usa `useQuery` com `initialData` do cache para exibir lista paginada.
- Cache/invalidations cobertos pelos testes de fluxo completo em `tests/integration/transactions.e2e.test.ts` e unitários de filtros (evitam refetch desnecessário via chave normalizada).
