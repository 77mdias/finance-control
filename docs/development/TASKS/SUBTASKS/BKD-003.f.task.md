# BKD-003.f — Testes de integração E2E para CRUD Transactions

- **ID:** BKD-003.f
- **Título:** Testes de integração E2E cobrindo criação → leitura → atualização → deleção

## Checklist

- [x] Escrever cenário de teste que registra um user, cria transação, lista, atualiza e deleta
- [x] Validar respostas HTTP e estado no banco (Prisma)
- [x] Executar testes em CI (workflow) como smoke test

## Detalhes

- **Prioridade:** 🔴 Crítica
- **Estimativa:** 3h
- **Dependências:** BKD-001.g, BKD-003.c/d
- **Arquivos alvo:** `tests/e2e/transactions.*` ou `cypress/e2e/*`

### Notas de conclusão

- Fluxo E2E coberto em `tests/integration/transactions.e2e.test.ts` (cria → lista → atualiza → deleta com validação de `balanceDelta` e estado persistido no client Prisma simulado).
- Testes executados via `npm test` (Vitest) e incluídos no workflow de CI (`.github/workflows/ci.yml`).
