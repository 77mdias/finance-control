# Configuration Notes
- Local config via .env; database URL is VITE_DATABASE_URL.
- Neon Vite plugin can create a claimable database on first npm run dev (expires ~72 hours).
- Prisma client output path is src/generated/prisma (non-default).
